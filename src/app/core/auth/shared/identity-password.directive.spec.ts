import { IdentityPasswordDirective } from './identity-password.directive';

describe('IdentityPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
